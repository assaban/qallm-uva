[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "qallm-uva"
version = "0.2.0"
description = "Execution-Based Quality Assessment of AI-Generated Code in Jupyter Notebooks"
requires-python = ">=3.10"
dependencies = [
    "radon",
    "bandit",
    "ruff",
    "nbformat",
    "hypothesis",
    "pytest",
    "pytest-cov",
    "pytest-json-report",
    "coverage",
    "scipy",
    "openai",
    "anthropic",
    "ollama",
    "gitPython",
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
]

[project.optional-dependencies]
# Install with: pip install -e ".[experiments]"
# Needed only for the HumanEvalFix validation experiment (qallm.experiments).
experiments = [
    "datasets>=2.18.0",
]

[project.scripts]
qallm = "qallm.run_qallm:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 88
target-version = "py310"
